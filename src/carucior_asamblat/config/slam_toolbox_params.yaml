slam_toolbox:
  ros__parameters:

    # --- Solver ---
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # --- Frames / topics (potrivit cu gazebo.launch.py + bridge) ---
    odom_frame: odom
    map_frame: map
    base_frame: base_footprint
    # IMPORTANT: subscriere la /scan_for_slam (republicat de scan_qos_relay
    # cu QoS Reliable+KeepLast(5), match cu default-ul slam_toolbox).
    # Topic-ul brut /scan vine prin parameter_bridge cu QoS care nu match-uia.
    scan_topic: /scan_for_slam
    use_map_saver: true
    mode: mapping

    # --- Sim time ---
    use_sim_time: true

    # --- General ---
    debug_logging: false             # OFF — mai putin zgomot, e activ acum
    throttle_scans: 1
    # IMPORTANT: 0.1s = 10Hz pentru map->odom; previne "saritura" robotului
    # in RViz cand SLAM corecteaza poziția. Mai puține update-uri = mai stabil
    transform_publish_period: 0.1
    map_update_interval: 2.0         # /map la 2s — suficient, evita oscilatii
    resolution: 0.05
    max_laser_range: 12.0            # potrivit cu lidar (max 12.0 in URDF)
    minimum_time_interval: 0.3       # nu procesa scanuri prea des
    transform_timeout: 2.0
    tf_buffer_duration: 30.0
    stack_size_to_use: 40000000
    enable_interactive_mode: false

    # --- Scan matching ---
    use_scan_matching: true
    use_scan_barycenter: true
    # Adauga nod nou doar dupa 0.2m/0.2rad — graph mai mic, drift mai mic.
    # Cu 0.0 (anterior) se adaugau noduri duplicate la fiecare scan → drift.
    minimum_travel_distance: 0.2
    minimum_travel_heading: 0.2
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 10.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5
    loop_search_maximum_distance: 3.0
    # Loop closing OFF in timpul testarii — provoaca "snap-uri" mari ale hartii
    # cand detecteaza ca a mai trecut prin loc. Reactiveaza dupa ce esti
    # multumit de stabilitate.
    do_loop_closing: false
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    # --- Correlation ---
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # --- Scan matcher ---
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0

    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
    min_pass_through: 2
    occupancy_threshold: 0.1
